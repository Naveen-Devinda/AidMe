import 'package:flutter/material.dart';

class Mentalmainpage extends StatelessWidget {
  const Mentalmainpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Mental Health")));
  }
}
